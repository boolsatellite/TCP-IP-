//
// Created by satellite on 2023-06-02.
//


//fdopen函数将文件描述符转换为FILE指针
/*
 #include <stdio.h>
 FILE * fdopen(int fildes , const char * mode);
 成功返回转换的FILE指针，失败返回NULL
 * fildes：需要转换的文件描述符
 * modes：将要创建FILE指针的模式信息，与fopen函数中打开方式相同，常用"r" 和 "w"

 */


//fileno函数将FILE指针转换为文件描述符

/*
 #include <stdio.h>
 int fileno(FILE *stream);
 成功返回转换后的文件描述符，失败返回 -1
 */

























